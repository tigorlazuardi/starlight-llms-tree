name: Deploy docs
on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            package-lock.json
            docs/package-lock.json
      - run: npm ci
      - run: npm pack --pack-destination docs
      - run: npm ci
        working-directory: ./docs
      - run: npm install --no-save --package-lock=false ./starlight-llms-tree-*.tgz
        working-directory: ./docs
      - run: npm run build
        working-directory: ./docs
      - uses: actions/upload-pages-artifact@v3
        with:
          path: ./docs/dist
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
